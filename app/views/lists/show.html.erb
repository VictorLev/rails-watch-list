<h1><%= @list.name%></h1>

<ul>
  <% @movies.each do |movie|%>
    <li><%= movie.title%></li>
  <% end%>
</ul>

<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.association :movie %>
  <%= f.input :comment %>
  <%= f.submit "Add movie", class: "btn btn-primary" %>
<% end %>
